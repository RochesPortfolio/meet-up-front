import React from "react";
import whiteLogo from '../assets/icons/whiteLogo.png'

const Logo = () => {
    return (
        <div className="logo">
            <div className="logo-icon">
                <img src={whiteLogo} />
            </div>
        </div>
    );
};

export default Logo;
